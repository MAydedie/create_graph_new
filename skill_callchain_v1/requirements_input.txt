请在 create_graph 里做一套独立的 v1.0 skill 化逻辑，不要影响现在已有的 project 到笔记到 agent 主链。目标是补上 xyz skill 相似度匹配 和 代码编辑循环调用链。先不用做到极致，也不需要前端，只要 agent + 提示词 + 多个 python 脚本串起来运行即可。每一步都要生成中间结果文件，下一个脚本能读上一步结果继续往后走，直到输出最终结果。还需要给一个简洁蓝图和工图计划，并能做整条流程测试。
